import 'package:flutter/cupertino.dart';

class Questions extends StatelessWidget {
  const Questions({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('الاسئله الشائعه'),);
  }
}
