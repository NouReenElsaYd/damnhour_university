import 'package:flutter/material.dart';

class ProtectionSafety extends StatelessWidget {
  const ProtectionSafety({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('الحمايه و الامان'),);
  }
}
