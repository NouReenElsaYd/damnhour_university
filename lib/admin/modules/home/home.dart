import 'package:flutter/material.dart';

class Admin_Home extends StatelessWidget {
  const Admin_Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ADMIN', style: TextStyle(fontSize: 48))),
    );
  }
}
